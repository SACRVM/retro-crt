namespace Retro.Crt.Tui;

// Skeleton marker for the package — keeps the assembly buildable
// while the real widgets, layout primitives, and event loop are
// being added stage by stage on top of the core's ScreenBuffer +
// ScreenRenderer + (planned) input infra. Removed once at least
// one public type ships from this package.
internal static class TuiAssemblyMarker;
